import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maeldeveloppement.tapasradar',
  appName: 'tapasradar',
  webDir: 'dist'
};

export default config;
